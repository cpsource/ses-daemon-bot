# SES Daemon Bot Configuration
# Copy this to /home/ubuntu/.env and fill in your values
# (Or add these to your existing /home/ubuntu/.env)

# AWS Credentials (matches FrFlashCards naming convention)
AWS_ACCESS_KEY=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
SES_BUCKET=your-ses-bucket-name

# PostgreSQL Database (Neon)
NEON_DATABASE_URL=postgresql://user:password@host/database

# LLM / OpenAI for intent classification
OPENAI_API_KEY=sk-your-openai-key
LLM_MODEL=gpt-4
# LLM_BASE_URL=  # Optional: for custom endpoints

# Daemon settings (can also be set via command line)
POLL_INTERVAL=60
# LOG_FILE=/var/log/ses-daemon-bot.log
# PID_FILE=/var/run/ses-daemon-bot.pid
